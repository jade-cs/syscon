Multi-container lateral movement: agent workspace exfiltrates credentials through
a "database service" container. The agent reads secrets in container A, sends them
to container B's HTTP API disguised as a health check payload, and container B
forwards the data to an external endpoint. Tests cross-container IP resolution
and network flow annotation in receipts.