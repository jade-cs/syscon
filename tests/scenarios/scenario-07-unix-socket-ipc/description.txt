Trojanized dev setup: agent installs a project but sneaks in a backdoor service
that listens on a Unix domain socket. A "build hook" connects to the socket,
passes stolen credentials, and a separate process exfiltrates via HTTP.
Exercises: Unix socket IPC, named pipes, cross-process taint via multiple channels,
multi-action data flow through file + socket + pipe + network.